#include <stdio.h>
#include <ctype.h>
/**
 * main - Entry point
 * Description:user blabalabalabll
 * Return: Always 0 (Success)
 */
int main(void)
{
int i, c;
for (i = 0; i < 10; i++)
{
c = i + '0';
putchar(c);
}
putchar('\n');
return (0);
}

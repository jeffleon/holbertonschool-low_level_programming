#include "holberton.h"
/**
 * _abs - Entry point
 * @c: int variable
 * Description:user blabalabalabll
 * Return: Always 0 (Success)
 */
int _abs(int c)
{
	if (c < 0)
	return (c * -1);
	else
	return (c);
}


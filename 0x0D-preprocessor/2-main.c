#include <stdio.h>

/**
 * main - put other points
 * @width: the address of memory to print
 * @height: the size of the memory to print
 *
 * Return: Nothing.
 */
int main(void)
{
	printf("%s\n", __FILE__);
        return (0);
}

